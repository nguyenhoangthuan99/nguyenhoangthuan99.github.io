on:
  push:
    branches:
      - main

name: "Trigger: Push action"
permissions: {}

jobs:
  proselint:
    name: proselint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run ShellCheck
        uses: nguyenhoangthuan99/nguyenhoangthuan99.github.io@main

